diff --git a/.github/workflows/daily-tao.yml b/.github/workflows/daily-tao.yml
index 415147756ef12db7d87f0acaec8aff97f02f04f2..128183823dbf1c11bf6115e3a15b6f51a0f90496 100644
--- a/.github/workflows/daily-tao.yml
+++ b/.github/workflows/daily-tao.yml
@@ -1,33 +1,31 @@
 name: Daily TAO Earnings to Discord
 
 on:
   schedule:
     # Runs every day at 23:55 UTC. Adjust as needed.
     - cron: "55 23 * * *"
   workflow_dispatch: # allows manual run from GitHub UI
     inputs: {}
 
 jobs:
   post-daily-tao:
     runs-on: ubuntu-latest
 
     steps:
       - name: Checkout repo
         uses: actions/checkout@v4
 
       - name: Set up Python
         uses: actions/setup-python@v5
         with:
           python-version: "3.11"
 
-      - name: Install dependencies
-        run: |
-          python -m pip install --upgrade pip
-          pip install requests
-
       - name: Run daily TAO script
         env:
-          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
-          TAOSTATS_API_KEY: ${{ secrets.TAOSTATS_API_KEY }} # for your future Taostats integration
+          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL || 'https://discord.com/api/webhooks/1442323646197534751/e0jnw8jtVQBK1tXh2mwaEK7Vtw_DSrmucgbd50A7HRFvydZ5ZO6LocZSPtZ8mrNffT84' }}
+          TAOSTATS_API_KEY: ${{ secrets.TAOSTATS_API_KEY || 'tao-7cbff253-d6c9-4301-96fc-5e3e5e535acc:85e14a6e' }}
+          MINER_ADDRESSES: ${{ vars.MINER_ADDRESSES || secrets.MINER_ADDRESSES }}
+          TAO_LOOKBACK_DAYS: ${{ vars.TAO_LOOKBACK_DAYS || 1 }}
+          TAOSTATS_BASE_URL: ${{ vars.TAOSTATS_BASE_URL || 'https://taostats.io/api/v1' }}
         run: |
           python scripts/daily_tao_to_discord.py

        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          TAOSTATS_API_KEY: ${{ secrets.TAOSTATS_API_KEY }} # for your future Taostats integration
        run: |
          python scripts/daily_tao_to_discord.py
